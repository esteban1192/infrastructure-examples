pipeline {
    agent any
    stages {
        stage('Terraform Init') {
            steps {
                sh 'terraform --version'
                sh 'cd /home/jenkins/agent/aws/storage/efs-ec2-simple'
                sh 'terraform init'
            }
        }
    }
}
