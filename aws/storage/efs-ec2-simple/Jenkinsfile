pipeline {
    agent any
    stages {
        stage('Terraform Init') {
            steps {
                sh 'terraform --version'
                sh 'cd aws/storage/efs-ec2-simple'
                sh 'pwd'
            }
        }
    }
}
